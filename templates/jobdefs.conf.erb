JobDefs {
  Name = "<%= name -%>" 
  Type = "<%= type -%>"
  Level = "<%= level -%>"
  Client = "<%= client -%>"
  FileSet = "<%= fileset -%>" 
  Schedule = "<%= schedulee -%>" 
  Storage = "<%= storage -%>"
  Messages = "<%= messages -%>"
  Pool = "<%= pool -%>"
  Priority = "<%= priority -%>"
  Write Bootstrap = "<%= writebootstrap -%>" 
}

