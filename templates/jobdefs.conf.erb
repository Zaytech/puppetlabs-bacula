JobDefs {
  Name = "<%= name -%>" 
  Type = "<%= type -%>"
  Level = "<%= level -%>"
  Schedule = "<%= schedule_jobdef -%>" 
  Storage = "<%= storage -%>"
  Messages = "<%= messages -%>"
  Pool = "<%= pool -%>"
  Full Backup Pool = "<%= poolfull -%>"
  Incremental Backup Pool = "<%= poolinc -%>"
  Priority = "<%= priority -%>"
  Write Bootstrap = "<%= writebootstrap -%>" 
}

